query {
    accounts(pagination: {page: 0, onPage: 1}) {
        header {
            page
            onPage
            total
        }
        list {
            id
            currency
            amount
        }
    }
}
